full_domain: "mqtt.{{ hostname_root }}"

services_configs:
  - name: traefik
    required_folders:
      - logs
    touch_files:
      - acme.json
  - name: monitoring-client
  - name: filebrowser
    required_folders:
      - database
      - config
  - name: dozzle
